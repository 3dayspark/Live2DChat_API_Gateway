# ==========================================
# Live2DChat API Gateway Requirements
# ==========================================

# --- Web Framework & ASGI Server ---
# FastAPIとWebサーバー実行環境
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# --- Message Broker ---
# GPU Workerとの非同期通信用キュー
redis>=5.0.0

# --- LLM & External API Integration ---
# HTTPリクエスト (Gemini, Azure TTS, RVC API用)
requests>=2.31.0
# ModelScope / OpenAI 互換API呼び出し用
openai>=1.0.0

# --- Audio Processing & TTS ---
# Edge TTS 音声合成用
edge-tts>=6.1.9
# エラー時の無音(silence)WAVファイル生成用
soundfile>=0.12.1
numpy>=1.24.0

# --- Machine Learning (Emotion Detection) ---
# 感情分析モデル (BERT/Hubert) 実行用
transformers>=4.30.0
datasets>=2.14.0
scikit-learn>=1.3.0
# PyTorch (Transformersのバックエンド)
torch>=2.0.0